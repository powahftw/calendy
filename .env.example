# Firebase Web App config from Firebase Console -> Project settings -> General -> Your apps
# Required: sign-in is mandatory and settings sync through Firestore.
VITE_FIREBASE_API_KEY="your-firebase-web-api-key"
VITE_FIREBASE_AUTH_DOMAIN="your-project.firebaseapp.com"
VITE_FIREBASE_PROJECT_ID="your-project-id"
VITE_FIREBASE_STORAGE_BUCKET="your-project.firebasestorage.app"
VITE_FIREBASE_MESSAGING_SENDER_ID="your-messaging-sender-id"
VITE_FIREBASE_APP_ID="your-firebase-app-id"

# Google OAuth Web client ID used to read your calendar.
# Required: the Google Calendar API is Calendy's only source of events.
# Calendy requests the calendar.readonly scope, so it can read your calendars
# and events but can never create, modify or delete anything.
VITE_GOOGLE_CALENDAR_CLIENT_ID="your-google-oauth-client-id.apps.googleusercontent.com"
